syntax = "proto3";

package api;

message SurveyData{
    string id = 1;
    string type = 2;
    string study = 3;
    string description = 4;
}

service Survey{
    rpc CreateSurvey (SurveyData) returns (SurveyData);
}