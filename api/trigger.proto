syntax = "proto3";

package api;

import "user.proto";

service Check {
    rpc Trigger (Rule) returns (Rule) {}

    rpc CheckTrigger (UserMetaData) returns (Rule) {}
}


message Rule {
    repeated string condition = 1;
    string action = 2;
}
